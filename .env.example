# Public template only. Never put real credentials in this file.
#
# Future server-only secrets should be configured in Vercel's Environment
# Variables dashboard. Never expose a secret through a VITE_* variable.
#
# SERVER_API_KEY=
